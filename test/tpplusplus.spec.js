// var results = document.getElementById("qunit-fixture");

module("main", {
    setup: function() {
        
    }
});

test("when a tp button is on the page", function() {
    ok(true);
    // ok(results.firstChild.className.indexOf("btn-bookmarklet") > 0);
});